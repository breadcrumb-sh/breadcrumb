// Stub: SDK packages are empty during v3 rebuild. Real impl returns later.

export async function initBreadcrumb() {}

export function getTelemetry(
  functionId: string,
  metadata?: Record<string, string | number | boolean>,
) {
  return undefined;
}
