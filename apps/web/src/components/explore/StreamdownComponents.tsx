// Re-export from shared Markdown component.
// Kept for backward compatibility with existing imports.
export {
  MERMAID_THEMES,
  extractText,
  useHighlightedHtml,
  MermaidDiagram,
  CodeBlock,
  mdComponents as sdComponents,
} from "../common/Markdown";
